import { RandomTemplateDirective } from './random-template.directive';

describe('RandomTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
